const users: string[] = []; // this array will only hold strings
users.push("svk");

const bools: Array<boolean> = []; // another way to define an array

// 2D array
const board: string[][] = [
    ["x","y", "z"], ["a", "b", "c"]
]